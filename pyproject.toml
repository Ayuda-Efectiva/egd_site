[project]
name = "egd_site"
authors = [
    { name = "Ayuda Efectiva" }
]
description = "EGD Site"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "frappe",
]
